import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "All Bookmarks"
};

export default function BookmarksPage() {
    return (
        <div>
        <h1>Bookmarks</h1>
        </div>
    );
}